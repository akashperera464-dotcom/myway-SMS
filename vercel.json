{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "cd artifacts/myway && pnpm run build",
  "outputDirectory": "artifacts/myway/dist/public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
